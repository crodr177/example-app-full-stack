{
  "name": "example-app-full-stack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Application using Express.JS for backend and React.JS for frontend",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && cd server && npm start",
    "client": "cd client && yarn start",
    "server": "cd server && yarn start",
    "start": "concurrently \"npm run client\" \"npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
