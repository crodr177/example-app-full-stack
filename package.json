{
  "name": "example-app-full-stack",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack Application using Express.JS for backend and React.JS for frontend",
  "main": "index.js",
  "scripts": {
    "build": "cd client && yarn install && yarn build && cd .. && cd server && yarn install && yarn start",
    "heroku-postbuild": "cd server && npm install && cd .. && cd client && npm install && npm run build",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "cd server && npm start",
    "start-dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.3.0"
  }
}
